name: Build and Push to Azure Container Registry

on:
  push:
    branches:
      - main  # Runs on push to main branch

permissions:
  id-token: write
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        run: |
          docker buildx create --use
          docker buildx inspect --bootstrap

      - name: Log in to Azure
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Log in to Azure Container Registry
        run: |
          az acr login --name ${{ secrets.AZURE_REGISTRY_NAME }}

      - name: Build and tag Docker image for ARM64
        run: |
          docker buildx build --platform linux/arm64 -t ${{ secrets.AZURE_REGISTRY_NAME }}.azurecr.io/hello-world:arm64 --push .

      - name: Confirm image pushed to ACR
        run: |
          echo "Image has been pushed to ACR for ARM64 architecture"
